# 문제풀기 꿀팁

## 필요한 거 파악하기
1. 문제당 꼬리문제 3개
2. 변수 상세 파악
    - 유형, columns 개수
    - 필요한 라이브러리

## 테크닉
### ⭐️소수 둘째자리까지 출력하시오



```python
round(stat_A/stat_B, 2)
```
> - 결과 : 0.12
> - 값
> - 자리수

---
### ⭐️데이터 프레임 행,열 값

```python
dataframe.shape
```
> - 결과 : (19158, 15)
> - 행 개수
> - 열 개수

---
### ⭐️컬럼의 데이터 유형 바꾸기
#### 1. experience변수 값이 '>20' 또는 '<1'이 있는 행을 제거하고 experience변수의 유형을 정수로 변환하시오.
```python
df_base = df_base.loc[~df_base['experience'].isin(['>20','<1']), ]
df_base['experience']=df_base['experience'].astype('int')
```
> -astype : as type으로 'int','object','float' 가능

---
### 제거
#### 1. 분석에 사용하지 않을 컬럼을 제거하시오

```python
df = pd.read_csv("data.csv")
df_drop = df.drop(columns = ["city","company_size","company_type"])
```
> - columns : 리스트로 입력

#### 2. 각 문자형 변수에 결측치가 하나라도 존재하는 행은 모두 제거하시오

```python
df_base=df_base.dropna(subset=df_base.select_dtypes(include=['objects']).columns)

#series 출력
df_base.select_dtypes(include=
['int64','float64','objects'])
```
> subset : na가 있는지 확인할 열의 리스트

#### 3. 행에 특정 변수 값이 있을 때 제거하시오. 

```python
df_base = df_base.loc[~df_base['experience'].isin(['>20','<1']), ]
df_base['experience'] = df_base['experience'].astype('int')
```
> isin : list로 포함되는 값  
> isin 은 series로 동작함, 즉 dataframe임은 series의 묶음이니 가능하다.

---
### ⭐️ ~ 중 ~의 비율 구하기
```python
df_A = df_base.loc[df_base['relevant_experience'] == 'No relevant experience', ]
stat_A = df_A['target'].value_counts(normalize=True).get(1,0)
```
```python
df_base.loc[
    df_base['relevant_experience'] == 'No relevant experience', 
    ]['target'].value_counts(normalize=True).get(1,0)
```
> - value_counts : '특정 열'에서 각 값이 등장한 횟수를 세기만 함
>   - normalize=True : 비율을 구하고 싶을 때
>   - value_counts().get('원하는 값','없을 때 사용할 값')

### ⭐️ 더미변수 생성하기
#### 1. gender, relevant_experience, enrolled_university, education_level, major_discipline
변수로부터 더미 변수들을 생성한다. 단, 각 변수로부터 더미 변수를 생성할 때 마지막으로
등장하는 범주는 제외하도록 한다. (여기서 마지막으로 등장하는 범주란, 각 컬럼의 값을 사전
순으로 나열하였을 때 마지막으로 등장하는 값이다. 예를 들어, ‘col’ 변수의 범주가
「'A’,’C’,’B’,’B’,’C','A]의 값을 가진다면 사전 상의 마지막 값인 C가 제외된다)

```python
df_q2_obj_columns_list = df_base.columns.to_list()

df_dummy=pd.get_dummies(df_q2_obj, columns=df_q2_obj_columns_list,dtype='int')

df_dummy.drop(columns=[f"{col}_{sorted(df_q2_obj[col].unique())[-1]}" for col in df_q2_obj_columns_list], inplace=True)

```
> - pd.get_dummies(  
    1. 원핫인코딩으로 업데이트 할 df,  
    2. df 중에서 dummy로 만들 col,  
    3. 만들어질 dtype  
    )
> - 열을 미리 제거하는거보다 만든 후 drop으로 제거하는 것도 좋은 방법이다.  
    sorted('리스트')

---
### ⭐️ 더미변수 생성하기
#### 1. 더미 변수와 city_development_index, experience, last_new_job,
training_hours, target, Xgrp 변수를 결합하여 새로운 데이터셋을 구성한다. 이 때, target, Xgrp를 제외한 데이터셋의 컬럼은
아래 순서에 따르도로 하다.
```python
ls_col_x = ["city_development_index", "experience", "last_new_job", "training_hours"]
ls_col_y = ["target", "Xgrp"]
df_job2 = pd.concat([df_base[ls_col_y + ls_col_x], df_q2_dum], axis= 1)
```
> - pd.concat(  
    [합성할 dataframe의 리스트],  
    axis = 0(행)-아래로 붙이기, 1(열)-옆으로붙이기


---
### ⭐️ dataframe의 열 순서 바꾸기
#### 1. 원하는대로 바꾸기
```python
df = pd.DataFrame({"A":[1,2,3,4],
                   "B":[11,12,13,14], })
df = df[["B","A"]]
```
> - 단순하게 생각하자 열 리스트로 바꿔 넣는다
---
## 문제 유형
1. 로지스틱 회귀분석
- 분류문제
