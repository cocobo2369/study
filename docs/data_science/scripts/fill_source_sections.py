# -*- coding: utf-8 -*-
"""Fill source-order headings that previously contained only a mnemonic."""
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1] / "교재정리"

FILL = {
9:{"2. Join":"- **핵심:** 조인은 공통 키를 기준으로 두 표의 열을 결합한다. 키가 중복되면 행 수가 예상보다 늘어날 수 있으므로 조인 전후 행 수와 키 유일성을 확인한다.\n- **종류:** Inner는 양쪽 일치, Left/Right는 기준 쪽 전체, Full은 양쪽 전체, Cross는 모든 조합, Anti는 반대편에 없는 행을 남긴다.","3. 표본추출":"- **확률표본추출:** 단순무작위는 개체를 같은 확률로, 층화는 각 층에서, 군집은 군집 자체를, 계통은 일정 간격으로 뽑는다.\n- **시험 함정:** 층화는 모든 층을 반영해 정밀도를 높이고, 군집은 일부 군집만 뽑아 비용을 낮춘다."},
10:{"2. 수치형 데이터 변환의 적용":"- 거리·경사 기반 모델은 표준화 효과가 크지만, 나무 기반 모델은 단조 스케일 변환 영향이 작다.\n- `fit`은 학습 데이터에만 수행하고 검증·시험 데이터에는 같은 평균·표준편차를 적용해야 누수를 막는다."},
11:{"3. 상관분석의 쓰임새":"- 변수 관계 탐색, 중복 특성 점검, 후속 회귀분석 후보 선정에 활용한다. 시간 순서나 실험 설계가 없다면 상관만으로 원인을 결론 내리지 않는다.","4. 상관분석의 종류와 가정":"- 피어슨은 연속형·선형 관계, 스피어만은 순위·단조 관계에 적합하다. 산점도로 비선형성과 이상치를 먼저 확인한다.","5. 이변량 정규분포":"- 두 변수의 모든 선형결합이 정규분포를 따르는 결합분포다. 피어슨 상관의 유의성 검정과 신뢰구간에 필요한 강한 가정으로 이해한다.","6. 자기상관":"- 시계열에서 현재 값과 과거 값의 상관이다. 자기상관이 남으면 관측 독립 가정이 깨지고 표준오차가 과소평가될 수 있다.","10. 공분산의 개념":"- $\\operatorname{Cov}(X,Y)=E[(X-\\mu_X)(Y-\\mu_Y)]$. 양수면 함께 증가, 음수면 반대로 움직인다. 단위에 의존하므로 표준화한 값이 상관계수다."},
14:{"2. 목적과 특징":"- 설명 목적에서는 계수·신뢰구간·가정을, 예측 목적에서는 새 데이터 오차와 예측구간을 중점적으로 본다.","3. 쓰임새":"- 연속형 결과의 요인 관계 설명, 평균 반응 추정, 새 관측치 예측에 쓴다. 관측자료의 회귀계수를 곧바로 인과효과로 읽지 않는다."},
15:{"6. 수행 시 고려사항":"- 선형성·독립성·등분산성·오차 정규성뿐 아니라 영향점, 외삽, 누락변수, 변수선택 후 추론의 불확실성도 확인한다."},
16:{"3. 쓰임새":"- 질병 유무, 이탈 여부, 합격 여부처럼 결과가 두 범주일 때 확률 예측과 요인 해석에 사용한다.","4. 베르누이 시행":"- $Y\\in\\{0,1\\}$, $P(Y=1)=p$, $E(Y)=p$, $Var(Y)=p(1-p)$다. 로지스틱 회귀는 관측마다 다른 $p_i$를 설명한다.","5. 로지스틱 함수":"- $p=1/(1+e^{-z})$이며 $z=\\beta_0+\\beta^T x$다. 역변환하면 $\\log[p/(1-p)]=z$가 된다.","9. 예측값 산출":"- 먼저 확률을 계산하고 임계값 이상이면 1로 분류한다. 임계값을 낮추면 일반적으로 재현율은 오르고 특이도는 내려간다.","11. 수행 시 고려사항":"- 완전분리, 불균형, 비선형 로짓, 다중공선성, 영향점과 확률 보정을 확인한다."},
17:{"2. 목적과 특징":"- 모수식 없이 주변 사례를 직접 참조한다. 복잡한 국소 경계를 표현하지만 예측 비용과 메모리 사용이 크다.","3. 쓰임새":"- 분류는 다수결, 회귀는 이웃 반응값 평균으로 예측한다. 비슷한 사례가 비슷한 답을 가진다는 가정이 핵심이다.","7. 평가":"- 교차검증으로 $k$, 거리척도, 가중방식을 함께 선택한다. 시험 데이터는 선택이 끝난 뒤 한 번만 평가한다.","8. 수행 시 고려사항":"- 표준화, 결측치, 범주형 변수의 거리 정의, 차원의 저주, 클래스 불균형과 동률 처리 규칙을 정한다."},
18:{"3. 쓰임새":"- 분류·회귀뿐 아니라 사람이 읽을 수 있는 규칙 추출과 변수 상호작용 탐색에 활용한다.","7. 분류 예측값 산출":"- 새 관측치가 도달한 잎의 최빈 클래스 또는 클래스 비율을 사용한다. 비율은 확률처럼 쓰지만 보정 여부를 확인한다.","8. 회귀 예측값 산출":"- 새 관측치가 도달한 잎에 속한 학습 반응값의 평균을 예측값으로 쓴다.","9. 평가":"- 분류는 혼동행렬·AUC, 회귀는 MAE·RMSE 등을 쓰며 가지치기 전후 검증성능을 비교한다."},
19:{"4. 베이즈 정리":"- $P(C|x)\\propto P(C)P(x|C)$. 나이브 가정 아래 $P(x|C)=\\prod_j P(x_j|C)$로 분해한다.","8. 평가":"- 클래스 예측은 혼동행렬 기반 지표로, 확률 출력은 로그손실과 보정곡선으로 평가한다.","9. 수행 시 고려사항":"- 특성 분포에 맞는 변형 선택, 0도수 스무딩, 강한 특성 의존, 사전확률과 확률 과신을 확인한다."},
20:{"2. 목적과 특징":"- 정답 없이 비슷한 관측을 묶으며 중심이 군집 대표값이 된다. 초기값과 $k$에 따라 결과가 달라진다.","3. 쓰임새":"- 고객 세분화, 이미지 색상 압축, 문서 묶기와 후속 분석용 군집 라벨 생성에 사용한다."},
21:{"5. Feature Transformation":"- 로그·제곱근은 오른쪽 꼬리를 줄이고, 표준화는 척도를 맞추며, 원-핫 인코딩은 명목 범주를 열로 펼친다. 모든 변환은 학습 데이터 기준으로 적합한다."},
22:{"2. 목적과 특징":"- 단일 모델의 편향 또는 분산을 줄이고 일반화 성능을 높인다. 구성 모델의 오류가 서로 다를수록 결합 이득이 커진다.","4. 부트스트랩":"- 원자료와 같은 크기로 복원추출한다. 한 표본이 한 번도 뽑히지 않을 확률은 약 36.8%이며 이 표본이 OOB 평가에 쓰인다."},
23:{"3. 쓰임새":"- 전처리가 비교적 적은 표형 데이터의 분류·회귀 기준모델, 변수 중요도 탐색에 널리 쓴다.","4. 의사결정나무 복습":"- 나무는 분할로 공간을 사각 영역으로 나누고 각 잎에서 상수값을 예측한다. 깊은 나무는 낮은 편향과 높은 분산을 가진다.","7. 예측값 산출":"- 분류는 나무별 클래스 투표 또는 확률 평균, 회귀는 나무별 수치 예측 평균을 사용한다."},
}

for chapter, sections in FILL.items():
    path = next(ROOT.glob(f"{chapter:02d}_*.md"))
    text = path.read_text(encoding="utf-8")
    for heading, addition in sections.items():
        marker = "## " + heading
        start = text.index(marker)
        next_start = text.find("\n## ", start + len(marker))
        end = len(text) if next_start < 0 else next_start
        block = text[start:end]
        if addition.splitlines()[0] not in block:
            lines = block.splitlines()
            insert_at = next((i + 1 for i, line in enumerate(lines) if line.startswith("> **암기 한 줄:**")), 2)
            lines[insert_at:insert_at] = ["", addition]
            text = text[:start] + "\n".join(lines).rstrip() + "\n" + text[end:]
    path.write_text(text, encoding="utf-8")
